import type {Metadata} from 'next';import DrawdownCalculator from '@/components/DrawdownCalculator';
export const metadata:Metadata={title:'Drawdown Recovery Calculator',description:'Calculate the percentage gain required to recover from a trading or investment drawdown.'};
const d=[5,10,15,20,25,30,40,50,60,75,90];
export default function Page(){return <main><div className="eyebrow">Risk tool</div><h1 className="page-title">Drawdown Recovery Calculator</h1><p className="lead">A 20% loss needs more than a 20% gain to recover. Calculate the real recovery percentage instantly.</p><DrawdownCalculator/><article className="content"><h2>Why recovery gets harder</h2><p>After a loss, the account has a smaller capital base. If $10,000 falls 20% to $8,000, recovering the lost $2,000 requires a 25% gain on $8,000.</p><div className="table-wrap"><table><thead><tr><th>Drawdown</th><th>Gain required to recover</th></tr></thead><tbody>{d.map(x=><tr key={x}><td>-{x}%</td><td>+{(x/(100-x)*100).toFixed(2)}%</td></tr>)}</tbody></table></div></article></main>}